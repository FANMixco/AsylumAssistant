import { Injectable } from "@angular/core";

Injectable()
export class Globals{
  readonly availableLng = [ ['en', 'en-US'], ['es', 'es-MX'], ['ar', 'ar-SA'], ['fr', 'fr-FR'] ];
  readonly version: string = "0.0.2";
}

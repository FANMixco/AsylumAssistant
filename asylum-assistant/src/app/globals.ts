import { Injectable } from "@angular/core";

Injectable()
export class Globals{
    readonly availableLng = ['en', 'es'];
    readonly availableT2S = ['en-US', 'es-MX'];
    readonly version: string="0.0.1";
}
